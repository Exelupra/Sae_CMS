<!DOCTYPE html>
<html lang="fr">
<link rel="stylesheet" href="../src/css/style.css">

<body>
<header>
    <nav>
        <div><a href="{{ url_for('home') }}">Accueil</a></div>
        <div><a href="{{ url_for('article') }}">Articles</a></div>

        {% if user %}
            {% if user.admin == 1 %}
                <div><a href="{{ url_for('allUsers') }}">Utilisateurs</a></div>
                <div><a href="{{ url_for('allCategorie') }}">Categories</a></div>
            {% endif %}
            <div><a href="{{ url_for('articleByUser') }}">Mes Articles</a></div>
            <div><a href="{{ url_for('deconnexion') }}">Déconnexion</a></div>
        {% else %}
            <div><a href="{{ url_for('inscription') }}">Inscription</a></div>
            <div><a href="{{ url_for('connexion') }}">Connexion</a></div>
        {% endif %}

    </nav>
</header>

<section id="content">
    {% block content %}{% endblock %}
</section>
</body>
</html>
